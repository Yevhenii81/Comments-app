version: '3.8'

services:
  backend:
    build: ./backend/backend
    volumes:
      - uploads_data:/app/wwwroot/uploads
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  uploads_data: